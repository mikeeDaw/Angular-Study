import { PriceSignPipe } from './price-sign.pipe';

describe('PriceSignPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSignPipe();
    expect(pipe).toBeTruthy();
  });
});
